# Bluemoon
first of all i scan reconnaisance using netdiscover at get 3 packets
then i recon with ifconfig to determine which one is the victim to attack
next i recon using nmap to scan the target
them i used the dirbuster/dirb/gobuster 
next i gain access using netcat and get password for armour
then proof to get root
escalate privileges using sudo -l
mainttain access for passwordless armour to root
lastly clear track history
